export const MERGE_EPSILON_RATIO = 0.15;     // extra overlap tolerance (15% of smaller radius)
export const MERGE_COOLDOWN_MS   = 80;       // prevent double-merge on SAME body within this window
export const MERGE_MAX_REL_V     = 9999;     // allow merging at almost any relative speed
